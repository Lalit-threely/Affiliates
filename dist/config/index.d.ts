declare const config: {
    env: string;
    db: {
        host: string;
        port: number;
        database: string;
        user: string;
        password: string;
    };
};
export default config;
